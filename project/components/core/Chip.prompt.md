Pill button for quick replies and carousel refine/sort filters.

```jsx
<Chip onClick={…}>Show more</Chip>
<Chip variant="tonal" selected>Price ↑</Chip>
```

`outline` (white, used as quick-reply chips under messages) or `tonal` (purple-100, used in refine bars). `selected` fills solid purple. Press shrinks 95%.
