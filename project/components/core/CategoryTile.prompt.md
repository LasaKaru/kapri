Category / occasion tile — emoji wayfinding in a grid (Cakes 🎂, Flowers 🌹, Birthday 🎉).

```jsx
<CategoryTile emoji="🎂" label="Cakes" onClick={…} />
```

Emoji is intentional here (reads faster than a line icon for categories). Lifts + tints purple-50 on hover; press shrinks 95%.
